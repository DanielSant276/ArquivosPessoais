/* A interface desenhável deverá ser implementada por toda classe que
   representa uma figura que pode ser visível em tela */
interface Desenhável {
	// Método que desenha o objeto
	public void desenha();
}
